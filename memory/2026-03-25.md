# Session Notes — 2026-03-25

## EOLAS Signal Engine — Build Session

Built and deployed a full-stack perpetual trading signal platform for perps.eolas.fun.

### What was built
- **57 files** committed to `workspace/eolas-signals/`
- FastAPI backend with multi-exchange data ingestion (Binance, Bybit)
- 5-module signal engine: OI Divergence, Funding Rate, Liquidation, Momentum, Volatility
- Next.js frontend dashboard with live signals, confidence gauges, EOLAS trade CTAs
- PostgreSQL + Redis for persistence and caching
- WebSocket live feed
- Telegram alert bot (not yet configured)
- Docker Compose deployment

### Current state (end of session)
- ✅ Platform running locally via Docker (Colima on macOS Ventura)
- ✅ Market data feeding from Binance + Bybit (OKX/Coinglass disabled — API issues)
- ✅ Dashboard live at http://localhost:3000
- ✅ Signals showing with TEST thresholds

### TEST MODE settings (must restore before going live!)
These are currently set for testing only — NOT production values:

| Setting | Current (test) | Production value |
|---|---|---|
| MIN_CONFIDENCE_SCORE | 5 | 70 |
| momentum MIN_PRICE_MOVE_PCT | 0.1 | 0.6 |
| momentum MIN_VOLUME_RATIO | 1.0 | 1.3 |
| oi_divergence threshold | 0.3 | 1.5 |
| funding_rate THRESHOLD_HIGH_LONG | 0.00005 | 0.0005 |
| engine consensus | relaxed (simple majority) | strict (2 strong signals) |

Files to restore:
- `backend/app/config.py` → MIN_CONFIDENCE_SCORE
- `backend/app/signals/modules/momentum.py`
- `backend/app/signals/modules/oi_divergence.py`
- `backend/app/signals/modules/funding_rate.py`
- `backend/app/signals/engine.py`
- `.env` → MIN_CONFIDENCE_SCORE=70

### EOLAS DEX URL format (confirmed correct)
`https://perps.eolas.fun/perp/PERP_{SYMBOL}_USDC`
e.g. `https://perps.eolas.fun/perp/PERP_BTC_USDC`

### Decisions made
- Threshold staying at 5 intentionally — more signals = win rate stats build faster = credibility
- Telegram group working ✅
- NFA disclaimer already in footer + every Telegram alert
- Confidence % shown on every signal card so users self-filter

### TODO next session
1. Deploy to a VPS (Hetzner/DO) for 24/7 uptime — currently only runs when laptop is on
2. Verify EOLAS trade links work correctly on the live site
3. Consider Telegram command handler (/signals, /markets, /stats)

### How to restart after laptop shutdown
```bash
colima start
cd ~/.openclaw/workspace/eolas-signals
docker compose up -d
```
Then open http://localhost:3000
