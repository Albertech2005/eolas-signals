# 2026-04-02 — Session Notes

## Project: EOLAS Signal Intelligence (Trading Signals Dashboard)

### Stack
- **Frontend**: Next.js 14 (App Router), deployed on **Vercel**, repo at workspace root → `frontend/` subdirectory
- **Backend**: FastAPI + PostgreSQL + Redis, deployed on **Railway**
- **URL**: `eolas-signals-production.up.railway.app` (backend)
- Reset secret: `eolas-reset-2026`

### What we built/fixed today

#### Bug fixes
- Premature SL hits: added 10-minute grace period in `scheduler.py` before outcome checks begin
- `is_winner = null` showing as red "losing" in SignalCard — fixed with `=== true / === false` explicit checks
- Confidence threshold raised to 75 (was 40/55) — updated frontend labels, monitoring bar, score display
- Backend env var `MIN_CONFIDENCE_SCORE` user will update manually on Railway

#### Big feature drop (all in one commit)
- **Live P&L** on active signal cards (uses market data already in app)
- **Score breakdown** collapsible panel in SignalCard (5 modules with bars)
- **Entry zone** (±0.3%) instead of exact entry price
- **Rate-of-change arrows** (↑↓) on monitoring cards using sessionStorage
- **Watchlist** — star symbols on monitoring cards, saved to localStorage
- **Win streaks** — backend `/analytics/streaks` endpoint + frontend display with 🔥❄️
- **How It Works / Guide page** at `/how-it-works`
- **Improved tweet share text** with TP1, TP2, SL, confidence label
- Added "Guide" to nav (was "How It Works" but shortened for mobile)

#### Build/deployment fixes
- `CheckCircle` → `CircleCheck` (renamed in lucide-react v0.379)
- `tsconfig.json` was missing `"target"` — defaulted to ES3, broke all Set/iterator types → added `"target": "ES2020"`
- Removed unused imports (`motion`, `LiveSignal`) from page.tsx
- `Array.from(Set)` instead of `[...Set]` spread (ES target compatibility)
- Removed nested git repos from index (clawlett, eolas-signals, solana-token-bot etc.) — added to .gitignore
- `output: 'standalone'` removed from next.config.js — was breaking Vercel client-side navigation (RSC payload routing)
- Added `vercel.json` to frontend with explicit nextjs config
- Nav links converted from `<Link>` → `<a>` with `router.push()` + 300ms `window.location.href` fallback

#### Ongoing issue (unresolved at session end)
- Nav links still not working with single tap/click on Vercel
- Root cause confirmed: `output: 'standalone'` in next.config.js breaks Next.js App Router navigation on Vercel
- Fix deployed but user had trouble getting the latest code live on Vercel
- User disconnected/reconnected GitHub to Vercel — webhook may have broken
- Resolution: `git commit --allow-empty -m "trigger vercel redeploy" && git push`

### Key technical notes
- No root `package.json` in workspace — Vercel auto-detects `frontend/` subdirectory
- Vercel "Redeploy" button redeploys a SPECIFIC old deployment, NOT latest commit — user kept redeploying old broken code
- Railway free tier queues builds — don't push multiple commits rapidly; bundle changes then push once
- Signal expiry window: 24 hours
- Score modules: OI Divergence (25), Funding Rate (20), Liquidation (25), Momentum (20), Volatility (10) = 100 total
- Default SL: 3%, TP1: 5%, TP2: 9% (ATR-adjusted in volatile markets)
